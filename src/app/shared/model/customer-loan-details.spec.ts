import { CustomerLoanDetails } from './customer-loan-details';

describe('CustomerLoanDetails', () => {
  it('should create an instance', () => {
    expect(new CustomerLoanDetails()).toBeTruthy();
  });
});
