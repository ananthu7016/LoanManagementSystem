import { VerificationDetails } from './verification-details';

describe('VerificationDetails', () => {
  it('should create an instance', () => {
    expect(new VerificationDetails()).toBeTruthy();
  });
});
